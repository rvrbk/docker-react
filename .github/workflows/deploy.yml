name: test
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: docker build -t rvrbk/docker-react -f Dockerfile.dev .
    steps:
      - run: docker run -e CI=true rvrbk/docker-react npm run test